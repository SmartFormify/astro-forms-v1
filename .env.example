# This URL is deliberately public: browser form endpoints are not secrets.
PUBLIC_SMARTFORMIFY_ENDPOINT=https://api.smartformify.com/fe/YOUR_ENDPOINT_KEY
# Only use server-only variables for integrations that must not run in the browser.
SMARTFORMIFY_ENDPOINT=https://api.smartformify.com/fe/YOUR_ENDPOINT_KEY
